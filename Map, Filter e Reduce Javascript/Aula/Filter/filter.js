//SINTAXE
//Array.filter(callback) //callback será a função qe será executada a cada momento

const frutas = ["maçã", "laranja", "limão", "limão siciliano", "abacaxi"];
frutas.filter((fruta) => fruta.includes("limão"));
